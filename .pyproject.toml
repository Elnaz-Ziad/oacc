[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "oacc"
version = "0.1.0"
description = "Utilities for cleaning, analyzing, and visualizing the OACC dataset"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []  # add numpy, pandas, etc. as needed

[tool.setuptools.packages.find]
where = ["src"]

